# AI 자동화 웹앱 구현 가능성 및 우려 사항 리포트

**작성일**: 2025-12-05
**문서 번호**: 155
**목적**: 비개발자를 위한 AI 기반 문제 분할 자동화 웹앱 구현 타당성 분석

---

## Executive Summary

### 결론: 구현 가능 (현재 시스템 확장 방식)

| 항목 | 평가 | 설명 |
|------|------|------|
| **기술적 가능성** | ✅ 가능 | 현재 FastAPI + React 스택 활용 |
| **UI/UX 구현** | ✅ 가능 | 기존 라벨링 UI 패턴 재사용 |
| **개발 기간** | 2~4주 | MVP 기준 |
| **주요 리스크** | 중간 | YOLOv8 서버 배포, OCR 비용 관리 |

---

## 1. 현재 시스템 분석

### 1.1 보유 자산

```
Frontend (React + TypeScript)
├── PageViewer.tsx      → 이미지 + 블록 시각화
├── PageCanvas.tsx      → 캔버스 렌더링, 드래그 선택
├── GroupPanel.tsx      → 그룹 편집 UI
├── workSessionStore.ts → 상태 관리 (Zustand)
└── Toss 스타일 디자인 시스템

Backend (FastAPI + Python)
├── density_analyzer.py → 블록 검출 (휴리스틱)
├── blocks.py           → 블록/그룹 API
├── pdf.py              → PDF 처리
└── export.py           → 문제 이미지 내보내기
```

### 1.2 현재 워크플로우

```
PDF 업로드 → 블록 자동 검출 → 수동 그룹핑 → 저장/내보내기
              (density_analyzer)   (PageViewer)
```

### 1.3 목표 워크플로우

```
PDF 업로드 → AI 자동 분할 → 검토/수정 → 저장/내보내기
              (YOLOv8+OCR)    (HITL UI)
```

---

## 2. 구현 가능성 분석

### 2.1 아키텍처 설계

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React)                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ PDF Upload  │ →  │ AI Preview  │ →  │ Review/Edit │     │
│  │    Page     │    │    Page     │    │    Page     │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                            ↓                                │
│                     confidence 표시                         │
│                     승인/수정 UI                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              ↓ API
┌─────────────────────────────────────────────────────────────┐
│                    Backend (FastAPI)                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ /api/ai/    │    │ /api/ai/    │    │ /api/ocr/   │     │
│  │   detect    │    │   predict   │    │   extract   │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         ↓                  ↓                  ↓             │
│    YOLOv8 모델      배치 처리 큐       Google Vision        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 핵심 컴포넌트별 구현 가능성

#### Frontend 컴포넌트

| 컴포넌트 | 구현 방법 | 난이도 | 재사용 가능 |
|----------|----------|--------|-------------|
| **AI 결과 시각화** | PageCanvas 확장 | 낮음 | ✅ 기존 코드 |
| **Confidence 표시** | 바운딩박스에 % 오버레이 | 낮음 | 신규 |
| **승인/거부 UI** | 버튼 + 키보드 단축키 | 낮음 | GroupPanel 패턴 |
| **수정 모드** | 기존 그룹핑 UI 재사용 | 낮음 | ✅ 기존 코드 |
| **배치 처리 진행률** | Progress bar + 상태 표시 | 낮음 | 신규 |

#### Backend 컴포넌트

| 컴포넌트 | 구현 방법 | 난이도 | 우려 사항 |
|----------|----------|--------|----------|
| **YOLOv8 추론** | ultralytics 라이브러리 | 중간 | GPU 필요 |
| **OCR 연동** | Google Vision API | 낮음 | API 키 관리 |
| **배치 처리** | Background Tasks / Celery | 중간 | 큐 관리 |
| **모델 로딩** | 싱글톤 패턴 | 낮음 | 메모리 관리 |

---

## 3. UI/UX 설계 (토스 철학 적용)

### 3.1 핵심 원칙

```
┌──────────────────────────────────────────────────────────────┐
│  원칙 1: AI First, Human Confirm                             │
│  - AI가 먼저 분할 제안                                        │
│  - 사용자는 승인/수정만 (Enter = 승인)                         │
├──────────────────────────────────────────────────────────────┤
│  원칙 2: Confidence 시각화                                   │
│  - 높은 신뢰도 (90%+): 초록색, 자동 승인 가능                   │
│  - 중간 신뢰도 (70-90%): 노란색, 검토 권장                     │
│  - 낮은 신뢰도 (<70%): 빨간색, 수정 필요                       │
├──────────────────────────────────────────────────────────────┤
│  원칙 3: 점진적 공개 (Progressive Disclosure)                 │
│  - 기본: AI 결과 + 승인 버튼만                                 │
│  - 수정 필요시: 상세 편집 모드 전환                            │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 화면 설계

#### 화면 1: 업로드 & AI 처리

```
┌─────────────────────────────────────────────────────────────┐
│  📄 AI 문제 자동 분할                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────────────────────────────────┐              │
│   │                                         │              │
│   │     PDF 파일을 드래그하거나 클릭         │              │
│   │                                         │              │
│   │         📁 파일 선택                    │              │
│   │                                         │              │
│   └─────────────────────────────────────────┘              │
│                                                             │
│   처리 옵션:                                                │
│   ☑️ 문제 번호 자동 추출 (OCR)                              │
│   ☐ 높은 신뢰도 자동 승인 (90% 이상)                        │
│                                                             │
│                    [ AI 분석 시작 ]                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 화면 2: AI 처리 진행

```
┌─────────────────────────────────────────────────────────────┐
│  🔄 AI가 문제를 분석하고 있어요                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   베이직쎈_공통수학1.pdf                                     │
│                                                             │
│   ████████████████░░░░░░░░  67%                            │
│                                                             │
│   ✅ 페이지 1-10 완료                                       │
│   🔄 페이지 11-15 처리 중...                                │
│   ⏳ 페이지 16-24 대기                                      │
│                                                             │
│   예상 남은 시간: 약 2분                                     │
│                                                             │
│   💡 처리가 완료되면 알림을 보내드릴게요                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 화면 3: 검토 & 수정 (핵심 UI)

```
┌─────────────────────────────────────────────────────────────┐
│  ✨ 24개 문제를 찾았어요!  검토가 필요한 항목: 3개            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────┐  ┌─────────────────────────────┐ │
│  │                      │  │  페이지 1 / 24              │ │
│  │   [시험지 이미지]     │  │  ━━━━━━━━━━━━━━━━━━━━━━━━  │ │
│  │                      │  │                             │ │
│  │   ┌────────────┐     │  │  ✅ 문제 1 (95%)           │ │
│  │   │ 문제 1     │ 95% │  │  ✅ 문제 2 (92%)           │ │
│  │   │ ┌────────┐ │     │  │  ⚠️ 문제 3 (67%) ← 현재    │ │
│  │   │ │ 보기   │ │     │  │  ✅ 문제 4 (89%)           │ │
│  │   │ └────────┘ │     │  │                             │ │
│  │   └────────────┘     │  │  추출된 정보:               │ │
│  │                      │  │  문제번호: 3                │ │
│  │   ┌────────────┐     │  │  영역: (120, 450, 800, 720) │ │
│  │   │ 문제 2     │ 92% │  │                             │ │
│  │   └────────────┘     │  ├─────────────────────────────┤ │
│  │                      │  │                             │ │
│  └──────────────────────┘  │  [✓ 승인] [✏️ 수정] [✗ 삭제] │ │
│                            │                             │ │
│  ◀ 이전    3 / 24   다음 ▶ │  단축키: Enter=승인, E=수정   │ │
│                            └─────────────────────────────┘ │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│  [ 모두 승인하고 저장 ]        검토 완료: 21/24             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 화면 4: 수정 모드 (기존 UI 재사용)

```
┌─────────────────────────────────────────────────────────────┐
│  ✏️ 문제 3 영역 수정                                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                                                      │  │
│  │   [기존 PageCanvas 컴포넌트 재사용]                   │  │
│  │                                                      │  │
│  │   - 드래그로 영역 조절                                │  │
│  │   - 블록 선택/해제                                   │  │
│  │   - 기존 그룹핑 UI 그대로                            │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  [ 취소 ]                              [ 수정 완료 ]        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 키보드 중심 워크플로우

```
검토 모드:
  Enter     → 현재 문제 승인 + 다음으로 이동
  E         → 수정 모드 진입
  Delete    → 현재 문제 삭제
  ←/→       → 이전/다음 페이지
  ↑/↓       → 이전/다음 문제
  Shift+A   → 모두 승인

수정 모드:
  Escape    → 수정 취소
  Ctrl+S    → 수정 저장
```

---

## 4. 기술 구현 상세

### 4.1 Backend: YOLOv8 통합

```python
# backend/app/services/ai_detector.py

from ultralytics import YOLO
from pathlib import Path
import numpy as np

class QuestionDetector:
    """문제 영역 자동 검출 서비스 (싱글톤)"""

    _instance = None
    _model = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._load_model()
        return cls._instance

    @classmethod
    def _load_model(cls):
        """YOLOv8 모델 로드 (서버 시작 시 1회)"""
        model_path = Path("models/question_detector.pt")
        if model_path.exists():
            cls._model = YOLO(str(model_path))
        else:
            # 기본 모델 사용 (초기 상태)
            cls._model = YOLO("yolov8m.pt")

    def detect(self, image_path: str, confidence: float = 0.5):
        """
        이미지에서 문제 영역 검출

        Returns:
            List[{
                "bbox": [x1, y1, x2, y2],
                "confidence": 0.95,
                "class": "Question_Block"
            }]
        """
        results = self._model(image_path, conf=confidence)

        detections = []
        for r in results:
            boxes = r.boxes
            for box in boxes:
                detections.append({
                    "bbox": box.xyxy[0].tolist(),
                    "confidence": float(box.conf[0]),
                    "class": "Question_Block"
                })

        return detections
```

### 4.2 Backend: OCR 통합

```python
# backend/app/services/ocr_service.py

from google.cloud import vision
import re

class OCRService:
    """Google Vision OCR 서비스"""

    def __init__(self):
        self.client = vision.ImageAnnotatorClient()

    def extract_problem_number(self, image_bytes: bytes) -> str | None:
        """
        이미지 상단에서 문제 번호 추출

        Returns:
            "1", "2", "3" 등 또는 None
        """
        image = vision.Image(content=image_bytes)
        response = self.client.text_detection(image=image)

        if response.text_annotations:
            text = response.text_annotations[0].description

            # 문제 번호 패턴 매칭
            patterns = [
                r'^(\d+)\s*[.번)]',      # "1.", "2번", "3)"
                r'^[문제]\s*(\d+)',       # "문제 1"
                r'^Q\.?\s*(\d+)',         # "Q1", "Q.2"
            ]

            for pattern in patterns:
                match = re.search(pattern, text, re.MULTILINE)
                if match:
                    return match.group(1)

        return None
```

### 4.3 Backend: API 엔드포인트

```python
# backend/app/routers/ai.py

from fastapi import APIRouter, UploadFile, BackgroundTasks
from ..services.ai_detector import QuestionDetector
from ..services.ocr_service import OCRService

router = APIRouter(prefix="/api/ai", tags=["AI"])

@router.post("/detect")
async def detect_questions(
    file: UploadFile,
    use_ocr: bool = True,
    confidence_threshold: float = 0.5
):
    """
    단일 페이지 문제 영역 검출

    Returns:
        {
            "page": 1,
            "detections": [
                {
                    "id": "det_001",
                    "bbox": [100, 200, 800, 600],
                    "confidence": 0.95,
                    "problem_number": "1"  # OCR 결과
                }
            ]
        }
    """
    detector = QuestionDetector()
    ocr = OCRService() if use_ocr else None

    # 이미지 저장 및 검출
    image_path = save_temp_image(file)
    detections = detector.detect(image_path, confidence_threshold)

    # OCR로 문제 번호 추출
    if ocr:
        for det in detections:
            cropped = crop_image(image_path, det["bbox"])
            det["problem_number"] = ocr.extract_problem_number(cropped)

    return {"detections": detections}


@router.post("/batch")
async def batch_detect(
    document_id: str,
    background_tasks: BackgroundTasks
):
    """
    전체 문서 배치 처리 (백그라운드)

    Returns:
        {"job_id": "job_123", "status": "processing"}
    """
    job_id = create_job_id()
    background_tasks.add_task(process_document, document_id, job_id)

    return {"job_id": job_id, "status": "processing"}


@router.get("/job/{job_id}")
async def get_job_status(job_id: str):
    """
    배치 작업 상태 조회

    Returns:
        {
            "job_id": "job_123",
            "status": "processing",
            "progress": 67,
            "completed_pages": 16,
            "total_pages": 24
        }
    """
    return get_job_progress(job_id)
```

### 4.4 Frontend: 검토 UI 컴포넌트

```typescript
// frontend/src/pages/AIReviewPage.tsx

import { useState, useCallback } from 'react';
import { useHotkeys } from 'react-hotkeys-hook';
import { PageCanvas } from '@/components/PageCanvas';

interface Detection {
  id: string;
  bbox: number[];
  confidence: number;
  problemNumber?: string;
  status: 'pending' | 'approved' | 'rejected' | 'modified';
}

export function AIReviewPage() {
  const [detections, setDetections] = useState<Detection[]>([]);
  const [currentIndex, setCurrentIndex] = useState(0);

  const currentDetection = detections[currentIndex];

  // 키보드 단축키
  useHotkeys('enter', () => approveAndNext());
  useHotkeys('e', () => enterEditMode());
  useHotkeys('delete', () => rejectCurrent());
  useHotkeys('left', () => prevPage());
  useHotkeys('right', () => nextPage());

  const approveAndNext = useCallback(() => {
    setDetections(prev => prev.map((d, i) =>
      i === currentIndex ? { ...d, status: 'approved' } : d
    ));
    setCurrentIndex(prev => Math.min(prev + 1, detections.length - 1));
  }, [currentIndex, detections.length]);

  const getConfidenceColor = (confidence: number) => {
    if (confidence >= 0.9) return 'text-green-500';
    if (confidence >= 0.7) return 'text-yellow-500';
    return 'text-red-500';
  };

  return (
    <div className="flex h-screen">
      {/* 왼쪽: 이미지 + 바운딩박스 */}
      <div className="flex-1 p-4">
        <PageCanvas
          detections={detections}
          currentDetection={currentDetection}
          onDetectionClick={(id) => setCurrentIndex(
            detections.findIndex(d => d.id === id)
          )}
        />
      </div>

      {/* 오른쪽: 검토 패널 */}
      <div className="w-80 border-l p-4">
        <h2 className="text-lg font-bold mb-4">
          검토 {currentIndex + 1} / {detections.length}
        </h2>

        {currentDetection && (
          <div className="space-y-4">
            <div className={getConfidenceColor(currentDetection.confidence)}>
              신뢰도: {(currentDetection.confidence * 100).toFixed(0)}%
            </div>

            <div>
              문제 번호: {currentDetection.problemNumber || '인식 실패'}
            </div>

            <div className="flex gap-2">
              <button
                onClick={approveAndNext}
                className="btn-primary flex-1"
              >
                ✓ 승인 (Enter)
              </button>
              <button
                onClick={enterEditMode}
                className="btn-secondary"
              >
                ✏️ 수정 (E)
              </button>
            </div>
          </div>
        )}

        {/* 전체 목록 */}
        <div className="mt-6">
          <h3 className="font-medium mb-2">전체 목록</h3>
          <ul className="space-y-1 max-h-64 overflow-y-auto">
            {detections.map((d, i) => (
              <li
                key={d.id}
                onClick={() => setCurrentIndex(i)}
                className={`p-2 rounded cursor-pointer ${
                  i === currentIndex ? 'bg-blue-100' : ''
                }`}
              >
                {d.status === 'approved' ? '✅' :
                 d.status === 'rejected' ? '❌' : '⏳'}
                {' '}문제 {d.problemNumber || '?'}
                <span className={getConfidenceColor(d.confidence)}>
                  {' '}({(d.confidence * 100).toFixed(0)}%)
                </span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  );
}
```

---

## 5. 우려 사항 및 대응 방안

### 5.1 기술적 우려

| 우려 사항 | 심각도 | 대응 방안 |
|----------|--------|----------|
| **YOLOv8 GPU 필요** | 높음 | CPU 추론 가능 (느림), 또는 클라우드 GPU |
| **모델 로딩 시간** | 중간 | 싱글톤 패턴으로 서버 시작 시 1회 로드 |
| **대용량 PDF 처리** | 중간 | 백그라운드 작업 + 진행률 표시 |
| **OCR API 비용** | 낮음 | 문제 번호 영역만 OCR (비용 최소화) |
| **학습 데이터 부족** | 중간 | 100~200페이지로 시작, 점진 확장 |

### 5.2 UX 우려

| 우려 사항 | 심각도 | 대응 방안 |
|----------|--------|----------|
| **AI 오류 시 사용자 혼란** | 중간 | 명확한 confidence 표시, 쉬운 수정 |
| **처리 대기 시간** | 중간 | 진행률 표시, 예상 시간 안내 |
| **기존 워크플로우와 혼란** | 낮음 | 별도 메뉴로 분리, 선택적 사용 |

### 5.3 운영 우려

| 우려 사항 | 심각도 | 대응 방안 |
|----------|--------|----------|
| **모델 버전 관리** | 중간 | models/ 폴더에 버전별 저장 |
| **API 키 보안** | 중간 | 환경 변수, .env 파일 |
| **서버 리소스** | 중간 | 동시 처리 제한, 큐 관리 |

---

## 6. 개발 로드맵

### Phase 1: 기반 구축 (1주)

```
□ YOLOv8 추론 서비스 구현
□ Google Vision OCR 연동
□ /api/ai/detect 엔드포인트
□ 단일 페이지 테스트
```

### Phase 2: 배치 처리 (3~4일)

```
□ 백그라운드 작업 큐
□ /api/ai/batch 엔드포인트
□ 진행률 조회 API
□ WebSocket 실시간 업데이트 (선택)
```

### Phase 3: 검토 UI (1주)

```
□ AIReviewPage 컴포넌트
□ 검토 패널 UI
□ 키보드 단축키
□ 수정 모드 (기존 UI 재사용)
```

### Phase 4: 통합 & 테스트 (3~4일)

```
□ 기존 시스템과 연결
□ 승인된 결과 → groups.json 저장
□ 내보내기 연동
□ 에러 처리 및 예외 케이스
```

---

## 7. 예상 비용

### 7.1 개발 비용

| 항목 | 비용 |
|------|------|
| 개발 시간 | 2~4주 (Claude Code) |
| 추가 라이브러리 | 무료 (ultralytics, google-cloud-vision) |

### 7.2 운영 비용 (월 1000페이지 기준)

| 항목 | 비용 |
|------|------|
| Google Vision OCR | $1.5 (약 2,000원) |
| 서버 (로컬) | 무료 |
| **합계** | **약 2,000원/월** |

---

## 8. 결론

### 8.1 구현 가능성: ✅ 높음

```
이유:
1. 현재 시스템 (React + FastAPI) 그대로 확장
2. 기존 UI 컴포넌트 (PageCanvas, GroupPanel) 재사용
3. YOLOv8, Google Vision 모두 Python 라이브러리 존재
4. 토스 스타일 디자인 시스템 이미 구축됨
```

### 8.2 주요 리스크

```
1. YOLOv8 초기 학습 데이터 필요 (100~200페이지)
2. GPU 없으면 추론 속도 느림 (페이지당 2~5초 → 10~20초)
3. OCR 정확도 실제 테스트 필요
```

### 8.3 권장 접근

```
1단계: 최소 기능 먼저 (2주)
       - YOLOv8 + 단일 페이지 검출
       - 간단한 검토 UI
       - 100페이지 라벨링으로 시작

2단계: 점진적 개선
       - 배치 처리 추가
       - OCR 연동
       - UX 개선
```

---

## 9. 다음 단계 제안

```
즉시 시작 가능:
□ YOLOv8 ultralytics 설치 테스트
□ Google Vision API 계정 생성
□ 50페이지 라벨링 (Roboflow 무료)
□ 첫 모델 학습 및 테스트

확인 후 결정:
□ 정확도 평가 → 추가 데이터 필요 여부
□ 속도 평가 → GPU 필요 여부
□ UI 프로토타입 → 사용성 검증
```

---

*작성: Claude Code (Opus 4.5)*
*마지막 업데이트: 2025-12-05*
