# Phase 33: RegistrationPage UX 재설계

> **목표**: 기존 RegistrationPage의 DualUploadCard 플로우를 개선하여 통합 캔버스로 연결
> **기준 페이지**: http://localhost:5173/
> **분석일**: 2025-12-03

---

## 1. 현재 UI 구조 분석

### 현재 RegistrationPage 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│  [헤더] 등록 & 라벨링                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔗 문제-해설 매칭                                       │   │
│  │                                                         │   │
│  │  ┌─────────────┐    ┌─────────────┐                    │   │
│  │  │  📄 문제    │    │  📖 해설    │   ◄── DualUploadCard│   │
│  │  │   PDF      │    │    PDF     │                    │   │
│  │  └─────────────┘    └─────────────┘                    │   │
│  │                                                         │   │
│  │         [매칭 시작하기]  ◄── 현재: /matching/...으로 이동│   │
│  │  ────────────────────────────────                       │   │
│  │  💻 듀얼 모니터로 작업하기                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────── 또는 파일을 하나씩 등록하세요 ───────────          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │     📁 파일을 드래그하거나 클릭하세요                     │   │
│  │        PDF, HWP, HWPX, HML 지원                         │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─ 진행 중인 라벨링 ─────────────────────────────────────────┐ │
│  │  📄 math_bible.pdf        [라벨링 대기]  [시작하기 →]      │ │
│  │  📄 solution.pdf          [라벨링 대기]  [시작하기 →]      │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 현재 플로우의 문제점

| 문제 | 설명 |
|------|------|
| **분리된 작업** | "시작하기" → `/labeling/:id` (개별 라벨링) |
| **매칭은 별도** | "매칭 시작하기" → `/matching/:p/:s` (매칭만, 라벨링 선행 필요) |
| **컨텍스트 단절** | 문제 라벨링 ↔ 해설 연결이 다른 페이지에서 진행 |

---

## 2. 제안: 통합 워크플로우

### 목표 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│  RegistrationPage (http://localhost:5173/)                       │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔗 문제-해설 매칭                                       │   │
│  │                                                         │   │
│  │  ┌─────────────┐    ┌─────────────┐                    │   │
│  │  │  📄 문제    │    │  📖 해설    │                    │   │
│  │  │   PDF      │    │    PDF     │                    │   │
│  │  └─────────────┘    └─────────────┘                    │   │
│  │                                                         │   │
│  │         [작업 시작하기]                                  │   │
│  │                 │                                       │   │
│  └─────────────────│───────────────────────────────────────┘   │
│                    │                                            │
│                    ▼                                            │
│         ┌──────────────────────┐                               │
│         │  통합 캔버스 페이지   │                               │
│         │  /work/:sessionId    │                               │
│         └──────────────────────┘                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 통합 캔버스 페이지 구조

```
┌──────────────────────────────────────────────────────────────────┐
│  [← 돌아가기]  작업명              [5/20 완료]  [완료]            │
├──────────────────────────────────────────────────────────────────┤
│   ┌──────────────┐  ┌──────────────┐                            │
│   │ 📄 문제 (12) │  │ 📖 해설 (8)  │    ◄── 탭 (그룹 수 표시)   │
│   └──────────────┘  └──────────────┘                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│                     ┌────────────────────┐                       │
│                     │                    │                       │
│                     │    현재 탭의       │                       │
│                     │    페이지 캔버스    │                       │
│                     │                    │                       │
│                     └────────────────────┘                       │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│   ◄ prev  │  p.3 / 20  │  next ►                                │
├──────────────────────────────────────────────────────────────────┤
│  미연결: [●1번] [2번] [3번] [4번] [5번] ... [+12번]              │
│          ▲ 현재 선택                                             │
└──────────────────────────────────────────────────────────────────┘
```

---

## 3. UI 변경 포인트

### 3.1 DualUploadCard 수정

**현재**:
```typescript
// 매칭 시작 → /matching/:problemDocId/:solutionDocId
const handleStartSingleTabMatching = () => {
  navigate(`/matching/${problemDocId}/${solutionDocId}`);
};
```

**변경 후**:
```typescript
// 작업 시작 → 세션 생성 → /work/:sessionId
const handleStartWork = async () => {
  // 1. 작업 세션 생성 (백엔드 API)
  const session = await api.createWorkSession({
    problemDocumentId,
    solutionDocumentId,  // 이제 처음부터 지정!
    name: `${problemDocName} - ${solutionDocName}`,
  });

  // 2. 통합 캔버스로 이동
  navigate(`/work/${session.sessionId}`);
};
```

### 3.2 버튼 텍스트 변경

| 현재 | 변경 후 |
|------|---------|
| "매칭 시작하기" | "작업 시작하기" |

**이유**: 이제 매칭만 하는 게 아니라 라벨링 + 매칭을 함께 하므로

### 3.3 세션 생성 API 수정

**현재** (Phase 32):
```typescript
// 문제 문서만 지정
createSession({
  problemDocumentId: string;
  problemDocumentName?: string;
  name?: string;
});
```

**변경 후**:
```typescript
// 양쪽 문서 동시 지정
createSession({
  problemDocumentId: string;
  problemDocumentName?: string;
  solutionDocumentId: string;    // 추가!
  solutionDocumentName?: string; // 추가!
  name?: string;
});
```

---

## 4. 라우트 변경

### 현재 (Phase 32)

```typescript
// App.tsx
<Route path="work/:sessionId/labeling" element={<WorkSessionLabelingPage />} />
<Route path="work/:sessionId/setup" element={<WorkSessionSetupPage />} />
<Route path="work/:sessionId/matching" element={<WorkSessionMatchingPage />} />
```

### 변경 후 (Phase 33)

```typescript
// App.tsx
<Route path="work/:sessionId" element={<UnifiedWorkPage />} />

// 기존 3개 페이지는 제거 또는 deprecated
```

---

## 5. 통합 캔버스 상세 설계

### 5.1 탭 전환 로직

```typescript
type ActiveTab = 'problem' | 'solution';

const [activeTab, setActiveTab] = useState<ActiveTab>('problem');
const [problemPage, setProblemPage] = useState(0);
const [solutionPage, setSolutionPage] = useState(0);

// 현재 페이지는 탭에 따라 다름
const currentPage = activeTab === 'problem' ? problemPage : solutionPage;
const setCurrentPage = activeTab === 'problem' ? setProblemPage : setSolutionPage;
```

### 5.2 탭 UI

```tsx
<div className="flex border-b border-grey-200">
  <button
    onClick={() => setActiveTab('problem')}
    className={cn(
      "px-6 py-3 font-medium transition-colors",
      activeTab === 'problem'
        ? "text-toss-blue border-b-2 border-toss-blue"
        : "text-grey-500 hover:text-grey-700"
    )}
  >
    <FileText className="w-4 h-4 mr-2 inline" />
    문제 ({problemGroups.length})
  </button>

  <button
    onClick={() => setActiveTab('solution')}
    className={cn(
      "px-6 py-3 font-medium transition-colors",
      activeTab === 'solution'
        ? "text-purple-600 border-b-2 border-purple-600"
        : "text-grey-500 hover:text-grey-700"
    )}
  >
    <BookOpen className="w-4 h-4 mr-2 inline" />
    해설 ({solutionGroups.length})
  </button>
</div>
```

### 5.3 하단 미연결 문제 바

```tsx
<div className="flex items-center gap-2 px-4 py-2 bg-grey-50 border-t overflow-x-auto">
  <span className="text-sm text-grey-500 flex-shrink-0">미연결:</span>

  {unlinkedProblems.map((problem) => (
    <button
      key={problem.groupId}
      onClick={() => setSelectedProblemId(problem.groupId)}
      className={cn(
        "px-3 py-1 rounded-full text-sm transition-colors flex-shrink-0",
        selectedProblemId === problem.groupId
          ? "bg-toss-blue text-white"
          : "bg-white border border-grey-200 text-grey-700 hover:border-toss-blue"
      )}
    >
      {problem.problemNumber}번
    </button>
  ))}

  {unlinkedProblems.length === 0 && (
    <span className="text-sm text-green-600">✓ 모두 연결됨</span>
  )}
</div>
```

### 5.4 그룹 생성 시 자동 연결

```typescript
// 해설 탭에서 그룹 생성 시
const handleGroupCreate = async (blockIds: number[]) => {
  if (activeTab === 'solution' && selectedProblemId) {
    // 1. 해설 그룹 생성
    const solutionGroup = await createGroup({
      documentId: solutionDocumentId,
      pageIndex: solutionPage,
      blockIds,
    });

    // 2. 자동 연결
    await createLink({
      problemGroupId: selectedProblemId,
      solutionGroupId: solutionGroup.id,
    });

    // 3. 다음 미연결 문제로 이동
    selectNextUnlinkedProblem();

    showToast(`${problemNumber}번 해설 연결 완료!`);
  } else {
    // 문제 탭에서는 일반 그룹 생성
    await createGroup({
      documentId: problemDocumentId,
      pageIndex: problemPage,
      blockIds,
    });
  }
};
```

---

## 6. 키보드 단축키

| 키 | 동작 | 조건 |
|----|------|------|
| `1` | 문제 탭으로 전환 | 항상 |
| `2` | 해설 탭으로 전환 | 항상 |
| `Tab` | 탭 토글 | 항상 |
| `←` `→` | 이전/다음 페이지 | 항상 |
| `↑` `↓` | 이전/다음 미연결 문제 | 문제 선택된 상태 |
| `G` | 선택된 블록으로 그룹 생성 | 블록 선택됨 |
| `Escape` | 선택 해제 | 항상 |

---

## 7. 구현 체크리스트

### 백엔드
- [ ] `POST /api/work-sessions` 수정: `solutionDocumentId` 처음부터 받기
- [ ] 기존 Step 로직 제거 (labeling → setup → matching 불필요)

### 프론트엔드
- [ ] `DualUploadCard.tsx`: "작업 시작하기" → 세션 생성 + `/work/:id` 이동
- [ ] `UnifiedWorkPage.tsx`: 새로운 통합 캔버스 페이지 생성
- [ ] `App.tsx`: 라우트 변경
- [ ] `workSessionStore.ts`: 탭 상태 추가 (`activeTab`, `problemPage`, `solutionPage`)
- [ ] 하단 미연결 문제 바 컴포넌트 생성

### 삭제/Deprecated
- [ ] `WorkSessionLabelingPage.tsx` (선택: 유지 또는 삭제)
- [ ] `WorkSessionSetupPage.tsx` (삭제)
- [ ] `WorkSessionMatchingPage.tsx` (로직은 통합 페이지로 이동)

---

## 8. 마이그레이션 영향

### 기존 세션 호환성

| 시나리오 | 처리 |
|----------|------|
| 기존 `step: 'labeling'` 세션 | `/work/:id`로 리다이렉트, 해설 없으면 선택 유도 |
| 기존 `step: 'setup'` 세션 | `/work/:id`로 리다이렉트, 해설 선택 필요 |
| 기존 `step: 'matching'` 세션 | `/work/:id`로 리다이렉트, 정상 동작 |

---

## 9. 결론

### 핵심 변경 사항

1. **DualUploadCard**: "매칭 시작하기" → "작업 시작하기" (세션 생성 + 통합 페이지)
2. **세션 생성**: 양쪽 문서 동시 지정
3. **통합 캔버스**: 탭 전환으로 문제/해설 오가며 작업
4. **하단 바**: 미연결 문제 시각화 + 빠른 선택

### 예상 결과

```
Before: 3페이지 이동 (labeling → setup → matching)
After:  1페이지에서 모든 작업 (탭 전환)
```

---

*리포트 작성: Claude Code (Opus)*
*마지막 업데이트: 2025-12-03*
