# Phase 37: 단일 저장소 통합 아키텍처 심층 연구 리포트

**작성일**: 2025-12-03
**유형**: 아키텍처 설계 연구
**범위**: 장기 프로젝트 (2-4주)

---

## Executive Summary

현재 시스템의 **두 개의 분산된 저장소**를 **단일 통합 저장소**로 재설계하는 방안을 심층 분석했습니다.

### 핵심 결론

| 항목 | 평가 |
|------|------|
| **구현 가능성** | ✅ 높음 (Option D 선택 시) |
| **예상 개발 기간** | 2-4주 |
| **위험도** | 🟡 중간 (마이그레이션 주의 필요) |
| **ROI** | ✅ 높음 (데이터 일관성, 유지보수성 향상) |

### 권장 방안: Option D (느슨한 결합 강화)

```
기존 구조 유지 + 동기화 레이어 추가
→ 최소 수정으로 완전한 데이터 일관성 달성
```

---

## 1. 현재 아키텍처 분석

### 1.1 저장소 분산 현황

```
┌─────────────────────────────────────────────────────────────────┐
│                     현재: 분산된 저장소                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────┐    ┌────────────────────────┐       │
│  │  documents/{id}/groups/ │    │  work_sessions/        │       │
│  │  (groups.json)          │    │  (session.json)        │       │
│  ├────────────────────────┤    ├────────────────────────┤       │
│  │ • 그룹 정의 (block_ids) │    │ • 세션 메타데이터      │       │
│  │ • 문항 정보 (problemInfo│    │ • problems[] (중복)    │       │
│  │ • link 필드 (연결정보)  │←?→ │ • links[] (연결정보)   │       │
│  │                         │    │                        │       │
│  │ 📁 페이지별 분리        │    │ 📁 세션별 단일 파일    │       │
│  │ 예: page_0001_groups.json│    │ 예: ws-123-abc.json   │       │
│  └────────────────────────┘    └────────────────────────┘       │
│              ↑                           ↑                       │
│              │                           │                       │
│       동기화 없음 ❌                 동기화 없음 ❌              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 데이터 중복 문제

```typescript
// groups.json의 ProblemGroup
{
  "id": "grp-abc123",
  "problemInfo": {
    "problemNumber": "1",
    "displayName": "1번"
  },
  "link": {                          // ← 연결 정보 #1
    "linkedGroupId": "grp-xyz789",
    "linkedDocumentId": "해설문서",
    "linkedPageIndex": 5
  }
}

// WorkSession의 데이터
{
  "problems": [{
    "groupId": "grp-abc123",         // ← 중복된 참조
    "problemNumber": "1",            // ← 중복된 데이터
    "displayName": "1번"             // ← 중복된 데이터
  }],
  "links": [{                        // ← 연결 정보 #2 (중복!)
    "problemGroupId": "grp-abc123",
    "solutionGroupId": "grp-xyz789",
    "solutionDocumentId": "해설문서",
    "solutionPageIndex": 5
  }]
}
```

**문제**: 같은 연결 정보가 **두 곳에 저장**되어 불일치 발생

---

## 2. 통합 아키텍처 옵션 비교

### 2.1 Option A: 모든 것을 groups.json에 통합

```
┌─────────────────────────────────────────────────────────────────┐
│  Option A: groups.json 중심 통합                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  documents/{id}/groups/page_XXXX_groups.json                    │
│  {                                                               │
│    "groups": [                                                   │
│      {                                                           │
│        "id": "...",                                              │
│        "block_ids": [...],                                       │
│        "problemInfo": {...},                                     │
│        "link": {...},              // 연결 정보                  │
│        "sessionId": "ws-xxx"       // 세션 참조 (신규)           │
│      }                                                           │
│    ],                                                            │
│    "sessionMeta": {                 // 세션 메타 임베드 (신규)   │
│      "step": "matching",                                         │
│      "status": "active"                                          │
│    }                                                             │
│  }                                                               │
│                                                                  │
│  work_sessions/ → 삭제                                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

| 장점 | 단점 |
|------|------|
| 단일 파일로 단순화 | 세션 정보가 페이지별로 분산됨 |
| 그룹과 연결이 함께 저장 | 세션 목록 조회 시 모든 파일 스캔 필요 |
| | 다중 세션 지원 어려움 |
| | groups.json 스키마 대폭 변경 |

**평가**: ❌ **비권장** - 세션 관리가 어려워짐

---

### 2.2 Option B: 모든 것을 WorkSession에 통합

```
┌─────────────────────────────────────────────────────────────────┐
│  Option B: WorkSession 중심 통합                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  work_sessions/ws-{id}.json                                     │
│  {                                                               │
│    "sessionId": "...",                                           │
│    "problemDocumentId": "...",                                   │
│    "solutionDocumentId": "...",                                  │
│                                                                  │
│    "problemGroups": [              // 그룹 정의 임베드           │
│      {                                                           │
│        "id": "...",                                              │
│        "pageIndex": 5,                                           │
│        "block_ids": [...],                                       │
│        "problemInfo": {...}                                      │
│      }                                                           │
│    ],                                                            │
│                                                                  │
│    "solutionGroups": [             // 해설 그룹도 임베드         │
│      {...}                                                       │
│    ],                                                            │
│                                                                  │
│    "links": [...]                  // 연결 정보                  │
│  }                                                               │
│                                                                  │
│  documents/{id}/groups/ → 읽기 전용 백업                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

| 장점 | 단점 |
|------|------|
| 세션 중심의 완전한 데이터 | 세션 파일이 매우 커짐 |
| 한 번의 로드로 모든 데이터 접근 | 페이지별 조회 비효율 |
| | 세션 없이 그룹 조회 불가 |
| | 대규모 마이그레이션 필요 |

**평가**: ⚠️ **주의** - 파일 크기와 성능 문제

---

### 2.3 Option C: 통합 데이터베이스 (SQLite)

```
┌─────────────────────────────────────────────────────────────────┐
│  Option C: SQLite 기반 통합                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  dataset_root/labeling.db                                       │
│                                                                  │
│  CREATE TABLE sessions (                                         │
│    id TEXT PRIMARY KEY,                                          │
│    problem_doc_id TEXT,                                          │
│    solution_doc_id TEXT,                                         │
│    status TEXT,                                                  │
│    created_at INTEGER                                            │
│  );                                                              │
│                                                                  │
│  CREATE TABLE groups (                                           │
│    id TEXT PRIMARY KEY,                                          │
│    session_id TEXT REFERENCES sessions(id),                     │
│    document_id TEXT,                                             │
│    page_index INTEGER,                                           │
│    block_ids TEXT,  -- JSON                                      │
│    problem_info TEXT  -- JSON                                    │
│  );                                                              │
│                                                                  │
│  CREATE TABLE links (                                            │
│    problem_group_id TEXT REFERENCES groups(id),                 │
│    solution_group_id TEXT REFERENCES groups(id),                │
│    linked_at INTEGER,                                            │
│    PRIMARY KEY (problem_group_id)                               │
│  );                                                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

| 장점 | 단점 |
|------|------|
| 완벽한 정규화 | 복잡도 대폭 증가 |
| 효율적인 쿼리 | 새로운 기술 스택 도입 |
| 트랜잭션 지원 | JSON 파일과 호환성 저하 |
| 인덱스로 빠른 검색 | NAS 동기화 시 충돌 위험 |
| | 개발 기간 4주 이상 |

**평가**: ❌ **비권장** - 오버엔지니어링, NAS 호환성 문제

---

### 2.4 Option D: 느슨한 결합 강화 (권장) ⭐

```
┌─────────────────────────────────────────────────────────────────┐
│  Option D: 느슨한 결합 + 동기화 레이어                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────┐    ┌────────────────────────┐       │
│  │  documents/{id}/groups/ │    │  work_sessions/        │       │
│  │  (기존 구조 유지)       │    │  (기존 구조 유지)      │       │
│  ├────────────────────────┤    ├────────────────────────┤       │
│  │ • groups[] (원본)       │    │ • problems[] (캐시)    │       │
│  │ • link (읽기용 캐시)    │←──→│ • links[] (원본) ⭐    │       │
│  │                         │ 동기화│                      │       │
│  └────────────────────────┘ 레이어└────────────────────────┘     │
│              ↑                           ↑                       │
│              │                           │                       │
│              └───────────┬───────────────┘                       │
│                          │                                       │
│                 ┌────────▼────────┐                              │
│                 │  Sync Manager   │                              │
│                 │  (새로 추가)    │                              │
│                 ├─────────────────┤                              │
│                 │ • 자동 동기화   │                              │
│                 │ • 충돌 해결     │                              │
│                 │ • 감사 로그     │                              │
│                 └─────────────────┘                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

| 장점 | 단점 |
|------|------|
| 기존 코드 최소 수정 | 동기화 로직 추가 필요 |
| 하위 호환성 100% | 두 저장소 유지 |
| 점진적 롤아웃 가능 | |
| 위험도 낮음 | |
| 개발 기간 2주 | |

**평가**: ✅ **강력 권장**

---

## 3. Option D 상세 설계

### 3.1 핵심 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                    Single Source of Truth                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  데이터 유형          │ 원본 저장소          │ 캐시 저장소       │
│  ─────────────────────┼─────────────────────┼─────────────────  │
│  그룹 정의 (block_ids)│ groups.json ⭐       │ -                 │
│  문항 정보            │ groups.json ⭐       │ session.problems  │
│  연결 정보            │ session.links ⭐     │ groups.json.link  │
│  세션 상태            │ session.json ⭐      │ -                 │
│                                                                  │
│  ⭐ = Single Source of Truth (유일한 원본)                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 동기화 규칙

```typescript
/**
 * 동기화 규칙
 *
 * 1. 그룹 생성/수정 시:
 *    groups.json 저장 → session.problems 캐시 업데이트
 *
 * 2. 연결 생성 시:
 *    session.links 저장 (원본) → groups.json.link 캐시 업데이트
 *
 * 3. 연결 삭제 시:
 *    session.links에서 제거 → groups.json.link 필드 null로
 *
 * 4. 세션 삭제 시:
 *    session.json 삭제 → groups.json.link 필드들 정리
 *
 * 5. 충돌 발생 시:
 *    session.links가 항상 우선 (authoritative)
 */
```

### 3.3 Sync Manager 설계

```python
# backend/app/services/sync_manager.py

class SyncManager:
    """
    groups.json ↔ WorkSession 동기화 관리자
    """

    async def sync_problems_to_session(
        self,
        session_id: str
    ) -> SyncResult:
        """
        groups.json → session.problems 동기화

        1. 문서의 모든 groups.json 스캔
        2. 신규 그룹 → session.problems에 추가
        3. 삭제된 그룹 → session.problems에서 제거
        4. 변경된 그룹 → session.problems 업데이트
        """
        pass

    async def sync_links_to_groups(
        self,
        session_id: str
    ) -> SyncResult:
        """
        session.links → groups.json.link 동기화

        1. session.links 순회
        2. 각 link에 대해 해당 groups.json 찾기
        3. groups.json의 link 필드 업데이트
        """
        pass

    async def full_sync(
        self,
        session_id: str
    ) -> SyncResult:
        """
        양방향 완전 동기화
        """
        result1 = await self.sync_problems_to_session(session_id)
        result2 = await self.sync_links_to_groups(session_id)
        return SyncResult.merge(result1, result2)

    async def resolve_conflict(
        self,
        session_id: str,
        strategy: ConflictStrategy = ConflictStrategy.SESSION_WINS
    ) -> SyncResult:
        """
        충돌 해결

        SESSION_WINS: session.links가 정답
        GROUPS_WINS: groups.json.link가 정답
        NEWEST_WINS: 타임스탬프 비교
        """
        pass
```

### 3.4 API 변경 사항

#### 기존 API 수정

```python
# work_sessions.py

@router.post("/{session_id}/links")
async def create_link(session_id: str, request: CreateLinkRequest):
    """
    연결 생성 (수정됨)

    1. session.links에 추가 (기존)
    2. ✨ 신규: groups.json.link 필드도 업데이트
    """
    session = _load_session(session_id)

    # 기존 로직
    link = ProblemSolutionLink(...)
    session.links.append(link)
    _save_session(session)

    # ✨ 신규: groups.json 동기화
    await sync_manager.sync_link_to_group(
        document_id=session.solutionDocumentId,
        group_id=request.solutionGroupId,
        link_data={
            "linkedGroupId": request.problemGroupId,
            "linkedDocumentId": session.problemDocumentId,
            # ...
        }
    )

    return session
```

#### 신규 API 추가

```python
# work_sessions.py

@router.post("/{session_id}/full-sync")
async def full_sync(session_id: str):
    """
    완전 동기화 API

    - groups.json의 모든 변경사항을 session에 반영
    - session.links를 groups.json에 반영
    """
    result = await sync_manager.full_sync(session_id)
    return {
        "status": "success",
        "problems_synced": result.problems_count,
        "links_synced": result.links_count,
        "conflicts_resolved": result.conflicts_count
    }

@router.get("/{session_id}/sync-status")
async def get_sync_status(session_id: str):
    """
    동기화 상태 확인

    Returns:
        status: "synced" | "pending" | "conflict"
        pending_changes: [...변경 목록...]
    """
    pass
```

### 3.5 프론트엔드 변경

```typescript
// workSessionStore.ts

interface WorkSessionStore {
  // 기존 상태
  currentSession: WorkSession | null;

  // ✨ 신규: 동기화 상태
  syncStatus: 'synced' | 'pending' | 'conflict';
  lastSyncAt: number | null;

  // ✨ 신규: 동기화 액션
  fullSync: () => Promise<void>;
  checkSyncStatus: () => Promise<void>;
}

// 자동 동기화 훅
export function useAutoSync(sessionId: string) {
  const { syncStatus, fullSync, checkSyncStatus } = useWorkSessionStore();

  // 주기적 상태 확인 (30초마다)
  useEffect(() => {
    const interval = setInterval(checkSyncStatus, 30000);
    return () => clearInterval(interval);
  }, [sessionId]);

  // 동기화 필요 시 알림
  useEffect(() => {
    if (syncStatus === 'pending') {
      // UI에 "새로고침 필요" 표시
    }
  }, [syncStatus]);
}
```

---

## 4. 구현 로드맵

### 4.1 Phase 37-D1: 기반 구축 (1주)

```
┌─────────────────────────────────────────────────────────────────┐
│  Week 1: 동기화 레이어 기반 구축                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Day 1-2: SyncManager 클래스 구현                               │
│  ─────────────────────────────────────────                      │
│  [ ] sync_problems_to_session() 구현                            │
│  [ ] sync_links_to_groups() 구현                                │
│  [ ] full_sync() 구현                                           │
│  [ ] 단위 테스트 작성                                           │
│                                                                  │
│  Day 3-4: API 수정                                              │
│  ─────────────────────────────────────────                      │
│  [ ] create_link에 동기화 로직 추가                             │
│  [ ] delete_link에 동기화 로직 추가                             │
│  [ ] /full-sync 엔드포인트 추가                                 │
│  [ ] /sync-status 엔드포인트 추가                               │
│                                                                  │
│  Day 5: 통합 테스트                                             │
│  ─────────────────────────────────────────                      │
│  [ ] 전체 워크플로우 테스트                                     │
│  [ ] 엣지 케이스 테스트                                         │
│  [ ] 성능 테스트 (100개 문제 기준)                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 Phase 37-D2: 프론트엔드 통합 (1주)

```
┌─────────────────────────────────────────────────────────────────┐
│  Week 2: 프론트엔드 통합 및 UX                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Day 1-2: 상태 관리 수정                                        │
│  ─────────────────────────────────────────                      │
│  [ ] workSessionStore에 syncStatus 추가                         │
│  [ ] fullSync, checkSyncStatus 액션 추가                        │
│  [ ] useAutoSync 훅 구현                                        │
│                                                                  │
│  Day 3-4: UI 피드백                                             │
│  ─────────────────────────────────────────                      │
│  [ ] 동기화 상태 인디케이터                                     │
│  [ ] "새로고침 필요" 토스트 알림                                │
│  [ ] 수동 동기화 버튼                                           │
│  [ ] 에러 상태 표시                                             │
│                                                                  │
│  Day 5: 테스트 및 버그 수정                                     │
│  ─────────────────────────────────────────                      │
│  [ ] E2E 테스트                                                 │
│  [ ] 사용자 시나리오 테스트                                     │
│  [ ] 버그 수정                                                  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 Phase 37-D3: 마이그레이션 (선택, 0.5주)

```
┌─────────────────────────────────────────────────────────────────┐
│  Week 3 (Optional): 기존 데이터 마이그레이션                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Day 1-2: 마이그레이션 스크립트                                 │
│  ─────────────────────────────────────────                      │
│  [ ] 기존 세션 스캔                                             │
│  [ ] groups.json ↔ session.links 일관성 검사                   │
│  [ ] 불일치 데이터 수정                                         │
│  [ ] 마이그레이션 리포트 생성                                   │
│                                                                  │
│  Day 3: 검증                                                    │
│  ─────────────────────────────────────────                      │
│  [ ] 마이그레이션 결과 검증                                     │
│  [ ] 롤백 테스트                                                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 구현 가능성 분석

### 5.1 기술적 실현 가능성

| 항목 | 난이도 | 근거 |
|------|--------|------|
| SyncManager 구현 | 🟢 낮음 | 단순한 파일 읽기/쓰기 로직 |
| API 수정 | 🟢 낮음 | 기존 엔드포인트에 2-3줄 추가 |
| 프론트엔드 상태 | 🟢 낮음 | Zustand 상태 필드 추가 |
| 동기화 UI | 🟡 중간 | 새로운 컴포넌트 필요 |
| 마이그레이션 | 🟡 중간 | 데이터 일관성 검증 필요 |

**종합 평가**: ✅ **충분히 실현 가능**

### 5.2 코드 변경 범위

```
┌─────────────────────────────────────────────────────────────────┐
│                      변경 파일 목록                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  백엔드 (Python)                                                │
│  ─────────────────────────────────────                          │
│  ✨ 신규: backend/app/services/sync_manager.py (~150줄)        │
│  📝 수정: backend/app/routers/work_sessions.py (~30줄 추가)    │
│  📝 수정: backend/app/routers/blocks.py (~20줄 추가)           │
│                                                                  │
│  프론트엔드 (TypeScript)                                        │
│  ─────────────────────────────────────                          │
│  📝 수정: frontend/src/stores/workSessionStore.ts (~50줄 추가) │
│  📝 수정: frontend/src/api/client.ts (~20줄 추가)              │
│  ✨ 신규: frontend/src/components/SyncIndicator.tsx (~30줄)    │
│  📝 수정: frontend/src/pages/UnifiedWorkPage.tsx (~10줄 추가)  │
│                                                                  │
│  총 변경량: 약 310줄 (신규 180줄 + 수정 130줄)                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 하위 호환성

```
┌─────────────────────────────────────────────────────────────────┐
│                    하위 호환성 보장                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ✅ 기존 API 시그니처 유지                                      │
│     - 모든 기존 엔드포인트 동일하게 작동                        │
│     - 응답 형식 변경 없음                                       │
│                                                                  │
│  ✅ 기존 데이터 형식 유지                                       │
│     - groups.json 스키마 변경 없음                              │
│     - session.json 스키마 변경 없음                             │
│                                                                  │
│  ✅ 점진적 활성화 가능                                          │
│     - 동기화 기능을 feature flag로 제어                         │
│     - 문제 발생 시 즉시 비활성화 가능                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. 우려 사항 및 위험 관리

### 6.1 기술적 위험

#### 위험 1: 동기화 경쟁 상태 (Race Condition)

```
┌─────────────────────────────────────────────────────────────────┐
│  시나리오: 동시 수정                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  User A: 그룹 1-5 매칭 중                                       │
│  User B: 같은 세션에서 그룹 6-10 매칭 중                        │
│                                                                  │
│  Timeline:                                                       │
│  T1: A가 그룹 3 매칭 → session.links 저장                       │
│  T2: B가 그룹 7 매칭 → session.links 저장                       │
│  T3: A의 sync_links_to_groups() 시작                            │
│  T4: B의 sync_links_to_groups() 시작                            │
│  T5: 두 동기화가 groups.json을 동시에 수정 → 💥 충돌           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**해결책**:
```python
# 파일 레벨 락 사용
import filelock

class SyncManager:
    def __init__(self):
        self.lock = filelock.FileLock("sync.lock")

    async def sync_links_to_groups(self, session_id: str):
        with self.lock:
            # 동기화 로직 (한 번에 하나만 실행)
            pass
```

**위험도**: 🟡 중간 → 🟢 낮음 (락 적용 후)

---

#### 위험 2: 대용량 데이터 성능

```
┌─────────────────────────────────────────────────────────────────┐
│  시나리오: 대규모 문서                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  문서: 500페이지 (예: 두꺼운 문제집)                            │
│  그룹: 페이지당 10개 = 5,000개 그룹                             │
│  groups.json 파일: 500개                                        │
│                                                                  │
│  full_sync() 실행 시:                                           │
│  - 500개 파일 읽기                                              │
│  - 5,000개 그룹 비교                                            │
│  - 예상 시간: 2-5초                                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**해결책**:
```python
# 1. 증분 동기화 (변경분만)
async def incremental_sync(self, session_id: str, since: int):
    """마지막 동기화 이후 변경분만 처리"""
    pass

# 2. 백그라운드 동기화
async def background_sync(self, session_id: str):
    """UI 블로킹 없이 백그라운드에서 실행"""
    asyncio.create_task(self.full_sync(session_id))

# 3. 캐시 활용
def get_groups_hash(self, document_id: str, page_index: int) -> str:
    """groups.json의 해시값으로 변경 여부 빠르게 확인"""
    pass
```

**위험도**: 🟡 중간 → 🟢 낮음 (최적화 적용 후)

---

#### 위험 3: 데이터 손실

```
┌─────────────────────────────────────────────────────────────────┐
│  시나리오: 동기화 중 시스템 크래시                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  T1: session.links 저장 완료                                    │
│  T2: groups.json 동기화 시작                                    │
│  T3: 💥 시스템 크래시                                           │
│  T4: 재시작 → session.links ≠ groups.json.link (불일치)        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**해결책**:
```python
# 1. 트랜잭션 로그
class SyncManager:
    async def sync_with_journal(self, session_id: str):
        journal_file = f"sync_journal_{session_id}.json"

        # 1. 저널에 작업 기록
        write_journal(journal_file, {"status": "started", "timestamp": now()})

        try:
            # 2. 동기화 실행
            await self.full_sync(session_id)

            # 3. 성공 기록
            write_journal(journal_file, {"status": "completed"})
        except Exception as e:
            # 4. 실패 시 롤백 가능
            write_journal(journal_file, {"status": "failed", "error": str(e)})
            raise

# 2. 시작 시 미완료 작업 복구
async def recover_pending_syncs(self):
    for journal in glob("sync_journal_*.json"):
        if journal["status"] == "started":
            await self.full_sync(journal["session_id"])
```

**위험도**: 🟡 중간 → 🟢 낮음 (저널링 적용 후)

---

### 6.2 운영적 위험

#### 위험 4: 마이그레이션 실패

```
┌─────────────────────────────────────────────────────────────────┐
│  시나리오: 기존 데이터 마이그레이션 중 오류                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  기존 세션 100개 마이그레이션 중                                │
│  세션 #47에서 오류 발생                                         │
│  → 47개 성공, 53개 미처리                                       │
│  → 시스템 불일치 상태                                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**해결책**:
```python
# 1. 드라이런 모드
async def migrate_with_dryrun(self):
    results = []
    for session in all_sessions:
        try:
            # 실제 저장 없이 검증만
            validation = await self.validate_sync(session.id)
            results.append({"session": session.id, "status": "ok"})
        except Exception as e:
            results.append({"session": session.id, "status": "error", "reason": str(e)})
    return results

# 2. 배치 마이그레이션 with 체크포인트
async def batch_migrate(self, batch_size: int = 10):
    checkpoint_file = "migration_checkpoint.json"

    # 이전 체크포인트에서 재개
    start_from = load_checkpoint(checkpoint_file)

    for i, session in enumerate(all_sessions[start_from:], start=start_from):
        await self.migrate_session(session)
        save_checkpoint(checkpoint_file, i + 1)
```

**위험도**: 🟡 중간 → 🟢 낮음 (롤백 계획 수립 후)

---

#### 위험 5: 사용자 혼란

```
┌─────────────────────────────────────────────────────────────────┐
│  시나리오: 동기화 지연으로 인한 UI 불일치                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. 사용자가 문제 3개 매칭                                      │
│  2. 동기화 지연 (네트워크 문제)                                 │
│  3. 미연결 바: 여전히 3개 미연결 표시                           │
│  4. 사용자 혼란: "방금 매칭했는데 왜 안 보이지?"               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**해결책**:
```typescript
// Optimistic UI + 동기화 상태 표시
function UnlinkedProblemsBar() {
  const { syncStatus, pendingLinks } = useWorkSessionStore();

  return (
    <div>
      {/* 동기화 중 표시 */}
      {syncStatus === 'pending' && (
        <Badge variant="warning">
          <Spinner size="sm" /> 동기화 중...
        </Badge>
      )}

      {/* Optimistic UI: 로컬에서 즉시 반영 */}
      {unlinkedProblems
        .filter(p => !pendingLinks.includes(p.groupId))
        .map(p => <ProblemChip key={p.groupId} {...p} />)
      }
    </div>
  );
}
```

**위험도**: 🟢 낮음 (Optimistic UI로 해결)

---

### 6.3 위험 요약 매트릭스

| 위험 | 발생 확률 | 영향도 | 대응 난이도 | 최종 위험도 |
|------|----------|--------|------------|------------|
| 경쟁 상태 | 중간 | 높음 | 낮음 (락) | 🟢 낮음 |
| 성능 저하 | 중간 | 중간 | 낮음 (캐시) | 🟢 낮음 |
| 데이터 손실 | 낮음 | 높음 | 중간 (저널) | 🟡 중간 |
| 마이그레이션 실패 | 중간 | 중간 | 낮음 (롤백) | 🟢 낮음 |
| 사용자 혼란 | 높음 | 낮음 | 낮음 (UI) | 🟢 낮음 |

**전체 위험도**: 🟢 **관리 가능**

---

## 7. 비용-효과 분석

### 7.1 개발 비용

| 항목 | 예상 시간 | 복잡도 |
|------|----------|--------|
| SyncManager 구현 | 8시간 | 중간 |
| API 수정 | 4시간 | 낮음 |
| 프론트엔드 수정 | 6시간 | 낮음 |
| 테스트 | 8시간 | 중간 |
| 마이그레이션 | 4시간 | 낮음 |
| **총계** | **30시간** (약 4일) | |

### 7.2 기대 효과

```
┌─────────────────────────────────────────────────────────────────┐
│                        기대 효과                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ✅ 데이터 일관성 100% 보장                                     │
│     - 새로고침 후에도 연결 정보 유지                            │
│     - 탭 전환 시 데이터 손실 없음                               │
│                                                                  │
│  ✅ 유지보수성 향상                                             │
│     - 디버깅 시 단일 원본 확인                                  │
│     - 데이터 불일치 이슈 제거                                   │
│                                                                  │
│  ✅ 사용자 경험 개선                                            │
│     - 작업 결과 영구 보관                                       │
│     - 신뢰할 수 있는 시스템                                     │
│                                                                  │
│  ✅ 확장성 확보                                                 │
│     - 다중 사용자 지원 기반                                     │
│     - 오프라인 동기화 기반                                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 ROI 평가

```
투자: 30시간 개발
효과:
  - 데이터 손실 버그 제거 (기존: 주 2-3건 예상)
  - 사용자 신뢰도 향상
  - 기술 부채 감소

ROI: ✅ 높음 (핵심 기능 안정성 확보)
```

---

## 8. 결론 및 권장 사항

### 8.1 최종 권장안

```
┌─────────────────────────────────────────────────────────────────┐
│                    권장: Option D 선택                          │
│              (느슨한 결합 강화 + 동기화 레이어)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  이유:                                                          │
│  1. 기존 코드 최소 수정 (약 300줄)                              │
│  2. 하위 호환성 100% 보장                                       │
│  3. 점진적 롤아웃 가능                                          │
│  4. 위험도 낮음 (롤백 용이)                                     │
│  5. 개발 기간 단축 (2주)                                        │
│                                                                  │
│  대안 비교:                                                     │
│  - Option A (groups.json 통합): 세션 관리 어려움                │
│  - Option B (WorkSession 통합): 대규모 리팩토링 필요            │
│  - Option C (SQLite): 오버엔지니어링, NAS 호환성 문제           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 실행 계획

```
Week 1: Phase 37-D1 (백엔드 SyncManager)
Week 2: Phase 37-D2 (프론트엔드 통합)
Week 3 (선택): Phase 37-D3 (마이그레이션)
```

### 8.3 성공 기준

```
[ ] 새로고침 후에도 연결 정보 100% 유지
[ ] 탭 전환 시 데이터 불일치 0건
[ ] 성능 저하 없음 (동기화 < 1초)
[ ] 기존 API 하위 호환성 100%
```

---

**승인 시 "Phase 37-D 진행해줘"로 구현 시작**
