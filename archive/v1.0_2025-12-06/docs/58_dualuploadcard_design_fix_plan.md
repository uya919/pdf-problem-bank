# DualUploadCard 디자인 수정 계획

**작성일**: 2025-12-02
**관련 문서**: 57_registration_page_ux_review.md

---

## 문제 분석

### 현재 상태

```tsx
<Card className="overflow-hidden">
  <CardHeader className="bg-toss-blue text-white">
    ...
  </CardHeader>
  <CardContent className="p-6">
    ...
  </CardContent>
</Card>
```

### 발견된 문제점

| 문제 | 원인 | 디자인 문법 위반 |
|------|------|-----------------|
| **헤더 상단 모서리가 직각** | Card의 default `padding="md"` (p-4)로 인해 CardHeader가 Card 안쪽에 배치됨 | `overflow-hidden`이 있어도 padding 내부라 적용 안됨 |
| **헤더-본문 radius 불일치** | CardHeader에 radius 미적용 | 토스: 내부 요소도 일관된 radius 사용 |

### 토스 디자인 문법 (tailwind.config.js)

```javascript
borderRadius: {
  'sm': '8px',      // 작은 요소
  'DEFAULT': '12px', // 기본값
  'md': '16px',     // Card 기본 (rounded-md)
  'lg': '20px',     // 큰 카드
  'xl': '24px',
  '2xl': '32px',
}
```

**Card 컴포넌트 기본값:**
- `rounded-md` (16px) - 토스 스타일

---

## 권장 해결책

### 옵션 A: 헤더 제거 + 미니멀 디자인 (권장)

토스 디자인 원칙 "Visual Simplicity"에 따라 불필요한 헤더를 제거하고 본문에 타이틀 통합.

```
┌─────────────────────────────────────────┐
│                                         │  ← rounded-md (16px)
│  🔗 문제-해설 매칭                       │  ← 타이틀 본문 통합
│  문제와 해설을 함께 업로드하세요          │  ← 설명 추가
│                                         │
│  ┌─────────────┐  ┌─────────────┐      │
│  │ 📄 문제 PDF  │  │ 📖 해설 PDF  │      │  ← 드롭존
│  └─────────────┘  └─────────────┘      │
│                                         │
│  [ 듀얼 윈도우로 매칭 시작 ]             │
│                                         │
└─────────────────────────────────────────┘
```

**장점:**
- 토스 디자인 원칙 완전 준수
- Card 컴포넌트 기본 사용법 따름
- radius 문제 자동 해결
- 더 깔끔한 UI

**구현:**
```tsx
<Card>
  <div className="flex items-center gap-2 mb-4">
    <Link2 className="h-5 w-5 text-toss-blue" />
    <h3 className="text-headline-sm text-grey-900">문제-해설 매칭</h3>
  </div>
  <p className="text-body-sm text-grey-500 mb-6">
    문제와 해설 PDF를 함께 업로드하세요
  </p>
  {/* 드롭존... */}
</Card>
```

---

### 옵션 B: padding 제거 + 헤더 radius 적용

Card에서 padding을 제거하고 헤더에 상단 radius 적용.

```tsx
<Card padding="none" className="overflow-hidden">
  <CardHeader className="bg-toss-blue text-white p-4 rounded-t-md">
    ...
  </CardHeader>
  <CardContent className="p-6">
    ...
  </CardContent>
</Card>
```

**단점:**
- CardHeader에 `rounded-t-md` 직접 추가 필요
- Card 기본 사용법과 다름
- 색상 있는 헤더는 토스 스타일에 맞지 않음

---

## 선택: 옵션 A (미니멀 디자인)

### 구현 계획

#### 단계 1: CardHeader/CardContent 제거

```tsx
// Before
<Card className="overflow-hidden">
  <CardHeader className="bg-toss-blue text-white">
    <CardTitle className="flex items-center gap-2">
      <Link2 className="h-5 w-5" />
      문제-해설 매칭
    </CardTitle>
  </CardHeader>
  <CardContent className="p-6">
    ...
  </CardContent>
</Card>

// After
<Card padding="lg">
  {/* 타이틀 영역 */}
  <div className="flex items-center gap-2 mb-2">
    <Link2 className="h-5 w-5 text-toss-blue" />
    <h3 className="text-headline-sm text-grey-900">문제-해설 매칭</h3>
  </div>
  <p className="text-body-sm text-grey-500 mb-6">
    문제와 해설 PDF를 함께 업로드하세요
  </p>

  {/* 드롭존... */}
</Card>
```

#### 단계 2: import 정리

```tsx
// Before
import { Card, CardContent, CardHeader, CardTitle } from '../ui/Card';

// After
import { Card } from '../ui/Card';
```

---

## 예상 결과

### Before (현재)
```
┌────────────────────────────────────────┐
│█████████ 문제-해설 매칭 ██████████████│  ← 직각 모서리, 강한 파란색
├────────────────────────────────────────┤
│  [문제 PDF]     [해설 PDF]             │
│  [ 매칭 시작 ]                         │
└────────────────────────────────────────┘
```

### After (수정 후)
```
╭────────────────────────────────────────╮
│  🔗 문제-해설 매칭                      │  ← 둥근 모서리, 깔끔한 타이틀
│  문제와 해설 PDF를 함께 업로드하세요     │
│                                        │
│  [문제 PDF]     [해설 PDF]             │
│  [ 매칭 시작 ]                         │
╰────────────────────────────────────────╯
```

---

## 체크리스트

- [ ] CardHeader, CardContent, CardTitle import 제거
- [ ] Card에 `padding="lg"` 적용
- [ ] 타이틀을 본문에 통합
- [ ] 설명 문구 추가
- [ ] TypeScript 빌드 확인
- [ ] 브라우저 테스트

---

*작성: Claude Code (Opus)*
