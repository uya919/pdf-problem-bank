# Phase 40: 라벨링 시스템 정리 및 아코디언 패널 개발 계획

> 2025-12-04 | 시스템 전체 검토 후 작성

---

## 1. 현재 상태 요약

### 1.1 핵심 컴포넌트 (유지)

| 컴포넌트 | 파일 | 역할 | 상태 |
|----------|------|------|------|
| **PageViewer** | `pages/PageViewer.tsx` | 페이지 관리, 그룹 저장, 상태 동기화 | ✅ 핵심 |
| **PageCanvas** | `components/PageCanvas.tsx` | Konva 기반 블록 시각화 | ✅ 핵심 |
| **GroupPanel** | `components/GroupPanel.tsx` | 그룹 목록, 편집, 확정 | ✅ 핵심 (개선 대상) |
| **UnifiedWorkPage** | `pages/UnifiedWorkPage.tsx` | 통합 작업 페이지 | ✅ 권장 진입점 |

### 1.2 발견된 문제점

```
┌──────────────────────────────────────────────────────────────┐
│ 🗑️ 삭제 대상 (백업 파일)                                      │
├──────────────────────────────────────────────────────────────┤
│ • PageViewer.tsx.backup                                      │
│ • PageViewer.tsx.backup-20251126-144518                      │
│ • PageCanvas.tsx.backup                                      │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ ⚠️ 중복 컴포넌트 (통합 필요)                                  │
├──────────────────────────────────────────────────────────────┤
│ • components/matching/ProblemListPanel.tsx                   │
│ • components/unified/ProblemListPanel.tsx                    │
│ → 거의 동일한 기능, unified 버전만 유지                       │
│                                                              │
│ • components/matching/MatchingHeader.tsx                     │
│ • components/unified/MatchingHeader.tsx                      │
│ → 통합 가능                                                  │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ 📦 레거시 페이지 (하위호환 유지)                              │
├──────────────────────────────────────────────────────────────┤
│ • WorkSessionLabelingPage.tsx                                │
│ • WorkSessionSetupPage.tsx                                   │
│ • WorkSessionMatchingPage.tsx                                │
│ → UnifiedWorkPage로 리다이렉트 권장                          │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ 🔧 미사용 훅                                                 │
├──────────────────────────────────────────────────────────────┤
│ • hooks/useGroupManager.ts                                   │
│ → 설계만 존재, PageViewer가 직접 구현                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 2. 정리 작업 (선행 필수)

### Phase 40-A: 코드 정리 (30분)

#### Step 1: 백업 파일 삭제
```
[ ] pages/PageViewer.tsx.backup 삭제
[ ] pages/PageViewer.tsx.backup-20251126-144518 삭제
[ ] components/PageCanvas.tsx.backup 삭제
```

#### Step 2: 미사용 훅 정리
```
[ ] hooks/useGroupManager.ts 검토
    - 실제 사용 여부 확인
    - 미사용 시 삭제 또는 아카이브
```

#### Step 3: 중복 컴포넌트 통합 (선택적)
```
[ ] components/matching/ vs components/unified/ 비교
[ ] matching 폴더 컴포넌트 → unified로 통합 가능 여부 판단
    - 지금 당장은 스킵 가능 (기능에 영향 없음)
```

---

## 3. 아코디언 패널 개발 계획

### 3.1 목표 UI

```
┌─────────────────────────────────────────┐
│  📋 작업 페이지                          │
│ ┌─────────────────────────────────────┐ │
│ │ ▼ 17p (현재)                    4/7 │ │  ← 펼쳐진 상태
│ │   ┌─────────────────────────────┐   │ │
│ │   │ 1  베이직쎈_p17 ✓          │   │ │
│ │   │ 2  베이직쎈_p17 ✓          │   │ │
│ │   │ 3  베이직쎈_p17 ✓          │   │ │
│ │   │ 4  (편집중...)              │   │ │
│ │   │ 5                           │   │ │
│ │   │ 6                           │   │ │
│ │   │ 7                           │   │ │
│ │   └─────────────────────────────┘   │ │
│ └─────────────────────────────────────┘ │
│ ┌─────────────────────────────────────┐ │
│ │ ▶ 10p                       7/7 ✓  │ │  ← 접힌 상태
│ └─────────────────────────────────────┘ │
│ ┌─────────────────────────────────────┐ │
│ │ ▶ 8p                        5/5 ✓  │ │  ← 접힌 상태
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 3.2 핵심 변경 사항

| 현재 (GroupPanel) | 변경 후 (AccordionGroupPanel) |
|-------------------|------------------------------|
| 현재 페이지 그룹만 표시 | 작업한 모든 페이지 표시 |
| 페이지 전환 시 목록 초기화 | 이전 페이지는 접혀서 유지 |
| 전체 진행 상황 파악 불가 | 한눈에 진행 상황 파악 |

### 3.3 데이터 구조

```typescript
// 새로운 상태: 방문한 페이지 추적
interface PageGroupSummary {
  pageIndex: number;       // PDF 페이지 인덱스
  bookPage: number;        // 책 페이지 번호
  groups: ProblemGroup[];  // 해당 페이지의 그룹들
  totalGroups: number;     // 전체 그룹 수
  confirmedGroups: number; // 확정된 그룹 수
  isComplete: boolean;     // 모든 그룹 확정 여부
}

// 아코디언 상태
interface AccordionState {
  expandedPages: number[]; // 펼쳐진 페이지 목록 (보통 현재 페이지만)
  visitedPages: number[];  // 작업한 페이지 순서 (스택)
}
```

---

## 4. 구현 단계

### Phase 40-B: 아코디언 그룹 패널 (2시간)

#### Step 1: 데이터 레이어 (30분)
```
[ ] useVisitedPages 훅 생성
    - 방문한 페이지 목록 관리
    - localStorage 영속화 (세션 복원용)

[ ] useMultiPageGroups 훅 생성
    - 여러 페이지의 그룹 데이터 캐싱
    - TanStack Query 기반
```

**파일 생성:**
```
frontend/src/hooks/useVisitedPages.ts
frontend/src/hooks/useMultiPageGroups.ts
```

#### Step 2: UI 컴포넌트 (1시간)
```
[ ] AccordionGroupPanel 컴포넌트 생성
    - 기존 GroupPanel 로직 재사용
    - 아코디언 UI 구현 (Radix UI 또는 직접 구현)

[ ] PageSection 컴포넌트 생성
    - 펼쳐진 상태: 기존 GroupPanel 내용
    - 접힌 상태: 페이지번호 + 진행률 + 완료 아이콘

[ ] 애니메이션 적용
    - Framer Motion으로 접기/펼치기
    - 페이지 전환 시 자연스러운 전이
```

**파일 생성:**
```
frontend/src/components/AccordionGroupPanel.tsx
frontend/src/components/PageSection.tsx
```

#### Step 3: 통합 (30분)
```
[ ] PageViewer에 AccordionGroupPanel 연결
    - 기존 GroupPanel 대체
    - props 인터페이스 호환성 유지

[ ] 페이지 전환 로직 연동
    - currentPage 변경 시 아코디언 상태 업데이트
    - 이전 페이지 자동 접기
```

**파일 수정:**
```
frontend/src/pages/PageViewer.tsx
```

---

## 5. 상세 설계

### 5.1 AccordionGroupPanel Props

```typescript
interface AccordionGroupPanelProps {
  // 현재 페이지 정보
  currentPage: number;
  bookPage: number;

  // 현재 페이지 그룹 (기존과 동일)
  groups: ProblemGroup[];
  selectedBlocks: number[];

  // 이벤트 핸들러 (기존과 동일)
  onCreateGroup: () => void;
  onDeleteGroup: (groupId: string) => void;
  onGroupSelect: (blockIds: number[]) => void;
  onUpdateGroupInfo?: (groupId: string, problemInfo: ProblemInfo) => void;
  onConfirmGroup?: (groupId: string) => Promise<void>;

  // 새로운 props
  visitedPages: PageGroupSummary[];  // 작업한 페이지 목록
  onPageClick?: (pageIndex: number) => void;  // 접힌 페이지 클릭 시

  // 기존 props 유지
  defaultBookName?: string;
  defaultCourse?: string;
  autoEditGroupId?: string | null;
  onAutoEditComplete?: () => void;
  previousPageLastNumber?: string | null;
  confirmingGroupId?: string | null;
}
```

### 5.2 useVisitedPages 훅

```typescript
function useVisitedPages(documentId: string) {
  const [visitedPages, setVisitedPages] = useState<number[]>([]);

  // localStorage에서 복원
  useEffect(() => {
    const saved = localStorage.getItem(`visitedPages_${documentId}`);
    if (saved) setVisitedPages(JSON.parse(saved));
  }, [documentId]);

  // 페이지 방문 기록
  const markVisited = useCallback((pageIndex: number) => {
    setVisitedPages(prev => {
      if (prev.includes(pageIndex)) return prev;
      const updated = [...prev, pageIndex];
      localStorage.setItem(`visitedPages_${documentId}`, JSON.stringify(updated));
      return updated;
    });
  }, [documentId]);

  return { visitedPages, markVisited };
}
```

### 5.3 애니메이션 스펙

```typescript
// Framer Motion variants
const sectionVariants = {
  expanded: {
    height: 'auto',
    opacity: 1,
    transition: { duration: 0.25, ease: 'easeOut' }
  },
  collapsed: {
    height: 48, // 접힌 헤더 높이
    opacity: 1,
    transition: { duration: 0.2, ease: 'easeIn' }
  }
};

const contentVariants = {
  expanded: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.2, delay: 0.1 }
  },
  collapsed: {
    opacity: 0,
    y: -10,
    transition: { duration: 0.15 }
  }
};
```

---

## 6. 체크리스트

### Phase 40-A: 코드 정리
```
[ ] 백업 파일 3개 삭제
[ ] useGroupManager.ts 검토 및 정리
[ ] 빌드 확인 (npm run build)
```

### Phase 40-B: 아코디언 패널
```
[ ] useVisitedPages 훅 구현
[ ] useMultiPageGroups 훅 구현
[ ] AccordionGroupPanel 컴포넌트 구현
[ ] PageSection 컴포넌트 구현
[ ] PageViewer 통합
[ ] 애니메이션 적용
[ ] 테스트 (페이지 전환, 저장, 확정)
```

---

## 7. 예상 결과

### Before (현재)
```
오른쪽 패널:
┌─────────────────┐
│ 문제 그룹   0개  │
│                 │
│ [빈 상태]       │
│                 │
└─────────────────┘
→ 현재 페이지만 표시
→ 전체 진행 상황 파악 불가
```

### After (개선)
```
오른쪽 패널:
┌─────────────────┐
│ ▼ 17p      4/7  │ ← 현재 페이지 (펼침)
│   1 ✓           │
│   2 ✓           │
│   3 ✓           │
│   4 (편집중)    │
│ ▶ 10p  7/7 ✓   │ ← 완료 (접힘)
│ ▶ 8p   5/5 ✓   │ ← 완료 (접힘)
└─────────────────┘
→ 전체 작업 흐름 한눈에 파악
→ 이전 페이지 클릭으로 빠른 이동
```

---

## 8. 리스크 및 대응

| 리스크 | 확률 | 대응 |
|--------|------|------|
| 다중 페이지 데이터 로딩 지연 | 중 | 방문한 페이지만 로드 + 캐싱 |
| 기존 저장 로직 충돌 | 낮 | Phase 34-F 로직 그대로 유지 |
| 아코디언 많아지면 스크롤 | 중 | 최근 10개만 표시 + 더보기 |
| GroupPanel 호환성 | 낮 | props 인터페이스 확장 (기존 유지) |

---

## 9. 실행 순서

```
1. Phase 40-A 실행 (30분)
   ↓
2. 빌드 확인
   ↓
3. Phase 40-B Step 1 (30분)
   ↓
4. Phase 40-B Step 2 (1시간)
   ↓
5. Phase 40-B Step 3 (30분)
   ↓
6. 브라우저 테스트
   ↓
7. 완료
```

---

*작성: 2025-12-04*
